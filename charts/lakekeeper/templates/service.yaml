apiVersion: v1
kind: Service
metadata:
  name: {{ include "lakekeeper.fullname" . }}
  labels:
    {{- include "lakekeeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lakekeeper.selectorLabels" . | nindent 4 }}
